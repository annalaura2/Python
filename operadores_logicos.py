saldo = 200
cheque_esp = 200
conta = 0

# Operador E (and)
if(cheque_esp and saldo == 200): {
    print("iguais")
}

# Operador OU (or)
print(saldo >= cheque_esp or saldo <= conta)

# Operador de negação (not)
print(not saldo == 200)